using System;

namespace Kicad_utils
{
    // Note : default value should be first value
    // default is not always written to S-expression file

    //
    public enum SBool { no, yes };

    //
    public enum PageMode { landscape, portrait };

    //
    public enum Allowed { not_allowed, allowed };

}
